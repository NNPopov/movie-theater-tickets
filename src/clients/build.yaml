targets:
  $default:
    builders:
      json_serializable:
        options:
          # Nested objects (e.g. SeatLayout's bounds/screen/zones/seats) must be
          # serialized via their own toJson, so a toJson→fromJson round-trip is
          # lossless. Flat models are unaffected.
          explicit_to_json: true
