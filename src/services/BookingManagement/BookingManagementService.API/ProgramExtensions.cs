// Exposes the top-level-statement entry point as a public partial class so the
// WebApplicationFactory<Program> test harness can reference it. Program.cs (the
// composition root) is intentionally not edited.
public partial class Program
{
}
